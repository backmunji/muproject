import React from 'react'

const LogIn = () => {
  return (
    <div>
        <h2>로그인</h2>
        id:<input/>
        pw:<input type='password'/>
        <button>로그인</button>
        <button>회원가입</button>
    </div>
  )
}

export default LogIn